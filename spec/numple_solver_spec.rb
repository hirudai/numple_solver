require 'spec_helper'

describe NumpleSolver do
  it 'has a version number' do
    expect(NumpleSolver::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
